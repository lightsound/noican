# Rust
/target

# Downloaded model weights (never committed; see docs/models.md)
/models

# CLI batch-processing outputs
/out

# Build artifacts (dist/Noican.app, dist/Noican.driver)
/dist

# SwiftPM build directories
.build/

# macOS
.DS_Store
